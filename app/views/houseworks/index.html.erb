<h2>家事一覧<h2>
<div class="container">
  <% @housework.each do |housework| %>
    <div class="content" >
      <p><%= link_to housework.title, housework_path(housework.id) %></p>
      <span><%= housework.manner %></span>
    </div>
  <% end %>
</div>